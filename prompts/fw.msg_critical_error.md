This error has occurred: {{error_message}}

**Recovery instructions:**
1. Do NOT panic or start over. The error above is recoverable.
2. Check your current War Room Plan using `todo:list` to see where you were.
3. Resume working on the current in-progress task. If the error was caused by your last action, try an alternative approach.
4. If the same approach fails twice, use `think` with `mode: fast` to get advice on an alternative strategy.
5. Continue working through the todo list until all tasks are complete.